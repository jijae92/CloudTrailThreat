# CloudTrail Threat-Hunt Kit (.env.example)
# 실제 비밀값 금지! 운영환경에서는 AWS Secrets Manager/SSM Parameter Store 사용 권장.

# AWS 기본 설정 (더미)
AWS_REGION=us-east-1
ATHENA_DATABASE=cloudtrail_security
ATHENA_OUTPUT_BUCKET=s3://example-athena-output
GLUE_CATALOG_NAME=cloudtrail-logs

# Streamlit 로컬 개발
STREAMLIT_PORT=8501
STREAMLIT_DEBUG=true

# Scanner 기본 옵션
SCANNER_TEMPLATE=templates/app-sam.yaml
SCANNER_SOURCE=functions/vulnerable
SCANNER_OUTPUT=artifacts/scan.json

# CodePipeline/Lambda Hook 자리표시자
PRE_DEPLOY_HOOK_ARN=arn:aws:lambda:us-east-1:123456789012:function:guardrails-prehook
POST_DEPLOY_HOOK_ARN=arn:aws:lambda:us-east-1:123456789012:function:guardrails-posthook
